#include <stdio.h>
#include <string.h>

int main()
{

    // initializing strings
    char str1[] = "Hello World";
    char str2[] = "Using puts in C";

    // writing to stdout
    puts(str1);
    puts(str2);

    return 0;
}